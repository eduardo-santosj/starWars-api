/*
* This is an example file.
*	To use it, copy the content and create a file
* named: .config.json
*/

{
	"routes": {
		"source": "src/",
		"dist": "dist/"
	},

	"bs": {
		"browserToOpen": "google chrome",
		"portToStart": 3000,
		"urlToGo": "http://localhost/"
	},

	"output": {
		"cssFile": "style.min.css",
		"jsFile": "main.min.js"
	}
}